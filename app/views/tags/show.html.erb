<% unless @tag.nil? %>
  <div id="tag">
    <%= render "tags/tag", tag: @tag, show_untag_all: signed_in? %>
    <div class="clear"></div>
  </div>
  
  <div class="posts">
    <%= render "posts/posts", posts: @tag_posts, path: tag_path(@tag) %>
  </div>
<% end %>
