<% provide(:title, t(:user).pluralize) %><ul class="users">  <% @users.each do |user| %>    <li>      <!--if signed_in? && current_user.admin? && !user.admin?-->      <!--link_to "x", user, method: "delete", confirm: t(:confirm_delete_message), title: "#{t(:delete)} #{user.username}?"-->      <button class="close">&times;</button>      <!--end-->          <%= gravatar_for user %>      <%= link_to user.name, user %>    </li>  <% end %></ul>